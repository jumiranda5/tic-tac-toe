const Player = () => {

}

const gameBoard = (() => {

    const board = [
        "", "", "", 
        "", "", "", 
        "", "", ""
    ];

    const addMovement = (symbol, position) => {};

})();

// play against human or bot
// choose symbol (x will always start)
// add players movements
// show winner
// button restart